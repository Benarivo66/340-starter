<!-- <% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>
<%- messages()  %>
<div class="vehicleMgt">
    <a href="/inv/add-classification">Add new classification</a>
    <a href="/inv/add-inventory">Add new inventory</a>
</div>
<div class="selectClassification">
  <h2>Manage Inventory</h2>
  <p>Select a classification</p>
  <%- classificationList %>
  <table id="inventoryDisplay"></table>
  <noscript>JavaScript must be enabled to use this page.</noscript>
  <script src="../../js/inventory.js"></script>
</div> -->

<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/');
} %>

<%- messages() %>

<% if (locals.loggedin && locals.accountData && (locals.accountData.account_type === 'Employee' || locals.accountData.account_type === 'Admin')) { %>
  <div class="vehicleMgt">
      <a href="/inv/add-classification">Add new classification</a>
      <a href="/inv/add-inventory">Add new inventory</a>
  </div>

  <div class="selectClassification">
      <h2>Manage Inventory</h2>
      <p>Select a classification</p>
      <%- classificationList %>
      <table id="inventoryDisplay"></table>
      <noscript>JavaScript must be enabled to use this page.</noscript>
      <script src="../../js/inventory.js"></script>
  </div>
<% } else { %>
  <h2>You do not have permission to manage inventory.</h2>
<% } %>
